// ── Menu Tab Enum ────────────────────────────────────────────────────────────
// เพิ่มแท็บใหม่ที่นี่เพียงที่เดียว แล้วลงทะเบียน MenuTabRegistration ใน Inspector
// ของ MenuTabController — ไม่ต้องแก้โค้ดที่อื่นเลย
public enum MenuTabId
{
    Inventory,
    Quest,
    Relationships,
    Map,
    Skills,
    Achievements,
}
